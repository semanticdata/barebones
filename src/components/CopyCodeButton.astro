<script>
  import { initCopyCodeButtons } from "../lib/copyCode";

  function init() {
    initCopyCodeButtons();
  }

  document.addEventListener("DOMContentLoaded", init);
  document.addEventListener("astro:after-swap", init);
</script>
